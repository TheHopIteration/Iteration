import { createTheme } from '@mui/material'

const MuiTheme = createTheme({
    palette: {
        primary:{
            main: '#ee5d6c',
            contrastText: '#fff'
        }
    }
})

export default MuiTheme